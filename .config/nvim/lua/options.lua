require "nvchad.options"

vim.diagnostic.config {
    virtual_lines = true,
    virtual_text = false,
    severity_sort = true,
    underline = true,
    signs = true,
}

-- add yours here!

-- local o = vim.o
-- o.cursorlineopt ='both' -- to enable cursorline!
